{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Memory Build",
            "type": "shell",
            "command": "make build TARGET=memory MODULE_DIR=03-memory"
        },
        {
            "label": "Memory Watch",
            "type": "shell",
            "command": "make watch TARGET=memory MODULE_DIR=03-memory"
        }
    ]
}